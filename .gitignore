# Internal one-time tooling used to anonymize the original private data -- not needed for
# reproducing this repo (which reproduces forward from the already-anonymized data/ files),
# and it references a local path on the author's machine.
_build_scripts/

__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
